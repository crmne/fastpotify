# Finite scrolling comparison

Native Linux demo captures at 1200×800 and 1000×700, in both themes.
Both versions display the same fixture: 50 loaded tracks out of 1000.
Before: parent ed3b50c2 with only the finite-playlist demo fixture added.
After: ac589eae with finite scrolling. Later review fixes cover failure, cache,
filter and edit paths; they do not change these initial-view captures.
These are deterministic demo captures, not signed-in Spotify evidence.
